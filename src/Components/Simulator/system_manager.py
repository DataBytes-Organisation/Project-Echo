import os
import time

class SystemManager:
    def __init__(self):
        pass
    #     self.state = True if bool(os.environ['SYSTEM_START']) else False

    # def pause(self):
    #     while bool(os.environ['SYSTEM_PAUSE']):
    #         time.sleep(0.5)