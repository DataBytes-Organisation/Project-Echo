Download the data set to a folder and set the path in notebook file. 

#For Panns Cnnn14 model

run below code in cmd to download the model in windows. 

Invoke-WebRequest -Uri "https://zenodo.org/record/3987831/files/Cnn14_mAP%3D0.431.pth?download=1" -OutFile "path to download"

and set the path in file. 