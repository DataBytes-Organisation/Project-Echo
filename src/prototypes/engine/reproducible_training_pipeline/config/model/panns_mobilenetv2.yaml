name: panns_mobilenetv2
norm_choice: freeze_bn
params:
  model_name: "panns_mobilenetv2"
  pretrained: true
  num_classes: ${data.num_classes}
  use_qat: false
  trainable_blocks: -1
  use_arcface: true
