apiVersion: v1
kind: ConfigMap
metadata:
  name: api-env
data:
  DB_HOST: <set-in-secret>
  # Remove user/pass from ConfigMap; provide via Secret object instead
  DB_USER: <from-secret>
  DB_USER_PASS: <from-secret>
  DB_ROOT_USER: <from-secret>
  DB_ROOT_USER_PASS: <from-secret>
