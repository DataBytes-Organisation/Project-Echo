apiVersion: v1
kind: ConfigMap
metadata:
  name: engine-env
data:
  # Credentials now provided via Secret; only non-sensitive defaults here
  DB_USER: <from-secret>
  DB_USER_PASS: <from-secret>
  DB_ROOT_USER: <from-secret>
  DB_ROOT_USER_PASS: <from-secret>
  REDIS_HOST: redis
  DB_HOST: <from-secret>
  API_HOST: api
