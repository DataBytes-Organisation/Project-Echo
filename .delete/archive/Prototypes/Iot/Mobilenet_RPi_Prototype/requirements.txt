# Requirements for MobileNet Bird Detector

# IMPORTANT: Some packages require system libraries. On Debian/Ubuntu install these first:
#   sudo apt-get update
#   sudo apt-get install -y build-essential libsndfile1-dev portaudio19-dev libportaudio2
#   # Optional (easier PyAudio install):
#   sudo apt-get install -y python3-pyaudio
#
# Then create a virtualenv and install Python packages with:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# Core numeric and audio processing
numpy>=1.23,<2.0
scipy>=1.8,<2.0
librosa>=0.9.2
soundfile>=0.12
resampy>=0.3

# Audio I/O (PyAudio may also be provided by the OS package python3-pyaudio)
pyaudio>=0.2.11

# TensorFlow Lite runtime (preferred for small deployments)
# This package is platform-specific; if a wheel is not available for your platform,
# either install full TensorFlow or obtain a prebuilt tflite-runtime wheel for your platform.
tflite-runtime>=2.11.0

# Optional: full TensorFlow (uncomment if you need the full TF API instead of tflite-runtime)
# tensorflow>=2.11.0

# Image/spectrogram utilities
Pillow>=9.0

# ML utilities
scikit-learn>=1.0

# Development / testing (optional)
pytest>=7.0

# Optional, small helpful packages (uncomment if needed)
# matplotlib>=3.5
# pandas>=1.5
