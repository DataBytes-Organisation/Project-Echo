apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: project-echo
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
